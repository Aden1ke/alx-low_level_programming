#include <stdio.h>
#include <unistd.h>
/**
 * main - Write a program that prints the alphabet in lowercase.
 * Return:0 (Success)
 */
int main(void)
{
int x;
for (x = 'a'; x <= 'z'; a++)
{
putchar(x);
}
putchar('\n');
return (0);
}
