#include <stdio.h>
#include <unistd.h>
/**
 * main - Write a program that prints the alphabet in lowercase.
 * Return:0 (Success)
 */
int main(void)
{
for (x = 'a'; x <= 'z'; a++)
{
int x;
putchar(x);
}
putchar('\n');
return (0);
}
