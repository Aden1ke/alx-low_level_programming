#include <stdio.h>
/**
 * main - Write a program that prints the alphabet in lowercase.
 * Return:0 (Success)
 */
int main(void)
{
for (int x = 'a'; x <= 'z'; a++)
{
x = tolower(x)
putchar(x);
}
return (0);
}
