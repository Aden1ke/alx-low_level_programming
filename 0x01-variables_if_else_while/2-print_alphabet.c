#include <stdio.h>
/**
 * main - Write a program that prints the alphabet in lowercase.
 * Return:0 (Success)
 */
int main(void)
{
for (x = 'a'; x <= 'z'; a++)
{
int x = tolower(x)
putchar(x);
}
putchar('\n')
return (0);
}
