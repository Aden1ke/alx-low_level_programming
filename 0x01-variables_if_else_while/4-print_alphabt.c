#include <stdio.h>
#include <unistd.h>
/**
 * main - Write a program that prints the alphabet in lowercas, except q and e.
 * Return:0 (Success)
 */
int main(void)
{
int ch;
for (ch = 'a'; ch <= 'z' a++)
{
if (ch != 'q' && ch != 'e')
{
putchar;
}
else
{
}
}
putchar('\n');
return (0);
}
