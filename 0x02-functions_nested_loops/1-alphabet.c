#include <main.h>
/**
 * main - Write a function that prints the alphabet, in lowercase.
 * Return:0 (success)
 */
void print_alphabet(void)
{
char pat;
for (pat = a; pat <= z; pat++)
{
_putchar(pat);
}
putchar("\n");
return (0);
}
