#include "main.h"
#include <stdio.h>
/**
 * add - a function that adds two integers
 * and returns the result.
 * @a: first value
 * @b: second value
 * Return: Always 07.
 */
int add(int a, int b)
{
	return (a + b);
}
